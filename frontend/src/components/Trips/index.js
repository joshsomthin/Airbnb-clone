import React, { useState } from "react";

import "./Trips.css";

const Trips = () => {
  // We need to query the database for all instance of our reservations
  // We need to create cards for each reservation that can render and show details for our trip
  return (
    <div className="trips-container">
      <div className="hello">Welcome Josh!</div>
    </div>
  );
};

export default Trips;
